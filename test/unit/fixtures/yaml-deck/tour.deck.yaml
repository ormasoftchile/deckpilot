content: guide.md

deck:
  title: Widget Tour
  theme: dark
  split: heading

slides:
  - id: setup
    duration: 8s
    cues:
      - Walk through install
    actions:
      - type: terminal.run
        cmd: npm install widget

  - id: running
    notes: Show the running app here.

scenes:
  - name: start
    slide: widget
  - name: demo
    slide: running

recording:
  autoStart: false
  format: mp4
