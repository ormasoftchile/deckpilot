1
00:00:00,000 --> 00:00:03,500
Onboarding Mode Set `mode: onboarding` in
frontmatter to enable step-by-step
guidance with validation and checkpoints.
The progress bar at the bottom tracks
steps, not slide numbers.

2
00:00:03,500 --> 00:00:06,000
Onboarding Mode Set `mode: onboarding` in
frontmatter to enable step-by-step
guidance with validation and checkpoints.
The progress bar at the bottom tracks
steps, not slide numbers.

3
00:00:06,000 --> 00:00:08,500
Onboarding Mode Set `mode: onboarding` in
frontmatter to enable step-by-step
guidance with validation and checkpoints.
The progress bar at the bottom tracks
steps, not slide numbers.

4
00:00:08,500 --> 00:00:11,000
`validate.command` — Check Tools Validate
that a CLI tool is installed before
proceeding: If validation fails a Retry
button appears — the user stays on this
step until it passes.

5
00:00:11,000 --> 00:00:13,500
`validate.command` — Check Tools Validate
that a CLI tool is installed before
proceeding: If validation fails a Retry
button appears — the user stays on this
step until it passes.

6
00:00:13,500 --> 00:00:16,000
`validate.command` — Check Tools Validate
that a CLI tool is installed before
proceeding: If validation fails a Retry
button appears — the user stays on this
step until it passes.

7
00:00:16,000 --> 00:00:18,500
`validate.command` — Check Tools Validate
that a CLI tool is installed before
proceeding: If validation fails a Retry
button appears — the user stays on this
step until it passes.

8
00:00:18,500 --> 00:00:21,000
`validate.command` — Check Tools Validate
that a CLI tool is installed before
proceeding: If validation fails a Retry
button appears — the user stays on this
step until it passes.

9
00:00:21,000 --> 00:00:23,500
`validate.fileExists` — Check Files
Confirm required files are present:

10
00:00:23,500 --> 00:00:26,000
`validate.fileExists` — Check Files
Confirm required files are present:

11
00:00:26,000 --> 00:00:28,500
`validate.fileExists` — Check Files
Confirm required files are present:

12
00:00:28,500 --> 00:00:31,000
`validate.fileExists` — Check Files
Confirm required files are present:

13
00:00:31,000 --> 00:00:33,500
`validate.port` — Check Services Check
that a required service is reachable:
Start it first if needed:

14
00:00:33,500 --> 00:00:36,000
`validate.port` — Check Services Check
that a required service is reachable:
Start it first if needed:

15
00:00:36,000 --> 00:00:41,500
`validate.port` — Check Services Check
that a required service is reachable:
Start it first if needed:

16
00:00:41,500 --> 00:00:44,000
`validate.port` — Check Services Check
that a required service is reachable:
Start it first if needed:

17
00:00:44,000 --> 00:00:48,500
`validate.port` — Check Services Check
that a required service is reachable:
Start it first if needed:
