# Orchestration Log: De Unamuno — Sidecar Command Routing

**Timestamp:** 2026-04-22T23:40:53Z  
**Agent:** De Unamuno (general-purpose, background)  
**Task:** Implement sidecar-aware command routing

## Deliverable

**Status:** ✅ Complete

Implemented `resolveDeckUri()` helper in `src/extension.ts` to enable all deck-related commands to work from both `.deck.md` and `.deck.yaml` files. Updated 4 command handlers:

1. `executableTalk.openPresentation` — Opens presentation from sidecar
2. `executableTalk.validateDeck` — Validates deck from sidecar  
3. `deckpilot.extractMetadataToSidecar` — Regenerates sidecar when triggered from existing `.deck.yaml`
4. `deckpilot.showResolvedDeckModel` — Shows resolved model from sidecar

## Testing

- **Unit tests:** 877 passing, 0 failing
- **Compilation:** TypeScript strict mode ✅
- **Regression:** No breaking changes to existing `.deck.md` workflows

## Decision Rationale

- **Why not `when` clauses?** Handler-level routing is simpler and more flexible; no `package.json` changes required
- **File resolution:** Synchronous `fs.existsSync` check aligns with existing patterns (`extractMetadata.ts`, `showResolvedModel.ts`)
- **Error messaging:** Distinguishes "missing paired .deck.md" (triggered from sidecar) vs "open a deck file first" (no active editor)

## Upstream Dependencies

- **DA-03:** `sidecarLoader.ts` established `resolveSidecarPath()` pattern  
- **DA-06:** `parseDeck()` async auto-load of sidecar files
- **DA-23:** `extractMetadataToSidecar` command — now regeneration-aware

## Notes

Decision and implementation details recorded in `.squad/decisions/inbox/de-unamuno-sidecar-command-routing.md`.
